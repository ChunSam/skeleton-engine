# CLAUDE.md — skeleton-engine agent reference

> Version v1.6.225 | package `skeleton-engine` v0.131.0, library crate `engine` | wgpu-based Rust 2D game engine (wgpu 29, MSRV 1.95, CI pin Rust 1.95.0) | **Cargo workspace** (members `.` + `engine_reflect_derive` proc-macro)  
> WASM support: `cargo build --target wasm32-unknown-unknown` passes; an example ships to the web via `cargo build --example` + `wasm-bindgen` (see `examples/games/coin_race/web/`)  
> Full API: `REFERENCE.html` | dev history / architecture decisions: `docs/HANDOFF.md`  
> **Versioning: pre-1.0 (0.x)** — MINOR = any release (incl. breaking), PATCH = bugfix; 1.0.0 later. (Reset from 10.7.0, 2026-06-17, pre-publish — see CHANGELOG 0.11.0.)

---

## Conversation language

- **User-facing reports/questions → Korean; everything else → English** — agent-to-agent
  (subagent/Workflow prompts, handoffs), code, paths, identifiers, command output, and
  file-written docs (see Documentation rules). User ruling 2026-06-18; supersedes the harness default.

---

## Verification (run before declaring done)

A code/refactor change is **not done** until the **CI-equivalent** checks pass
*locally*. CI (`.github/workflows/ci.yml`) enforces these on every push, but run them
**before committing** so a regression never reaches `main`:

```bash
cargo fmt --check
cargo clippy --all-targets -- -D warnings
cargo build --target wasm32-unknown-unknown   # lib+bins — see wasm gotcha
cargo clippy --target wasm32-unknown-unknown --lib -- -D warnings  # wasm-only lints
cargo test --all-targets
cargo test --doc                                # --all-targets skips doctests
RUSTDOCFLAGS="-D warnings" cargo doc --no-deps  # CI fails docs on broken intra-doc links
```

Or run all of them in order via `./scripts/verify.sh`.

- **Read the gate's exit code, don't pipe it:** `./scripts/verify.sh | tail` (or any
  trailing pipe) reports `tail`'s `0` and **hides** a real `fmt --check`/`clippy` failure
  (bit twice). Capture it: `./scripts/verify.sh > /tmp/verify.log 2>&1; echo $?` (or
  `VERIFY_EXIT=$?`) is the authoritative verdict.
  - **zsh: `$pipestatus`, not `${PIPESTATUS[0]}`.** The shell here is **zsh**, where the
    pipe-status array is `$pipestatus` and is **1-indexed** — bash-style `${PIPESTATUS[0]}`
    is always the empty string (a whole session of `echo "X_EXIT=${PIPESTATUS[0]}"` silently
    printed `X_EXIT=`). Read a gate's code from a **non-piped** command
    (`cmd > /tmp/x.log 2>&1; echo $?`) or from a `run_in_background` task's completion
    notification (which reports the real code); if you must index a pipe, use `${pipestatus[1]}`.
- **WASM gotcha:** do *not* gate on `--target wasm32 --all-targets` — it fails on the
  native-only examples (`platformer_game`/`mp_server`/`gpu_particles`, which pull in
  `rapier2d`/`tungstenite`/`GpuParticleEmitter`). The lib+bins build above (or `--lib`)
  is the real wasm gate.
- **CI is ubuntu only** — `#[cfg(target_os = "macos")]`/`"windows"` code (+ OS-only deps like
  `objc2-game-controller`) is never compiled/run on CI, so **green CI alone doesn't verify an
  OS-gated change** (the macOS gamepad backend was merged on green CI **+** a hardware pad check).
  Build **both** cfg branches locally (`-D warnings`, esp. `dead_code`); one OS misses the other's lints.
- **Don't narrow the bar:** a prior "done" on only `fmt --check` + `test --lib` shipped the wasm-build + clippy regressions the full gate list above catches.
- **Optional wasm smoke checks** (non-CI; Chrome + `wasm-bindgen-cli`): `wasm_smoke.sh` /
  `wasm_save_smoke.sh` / `wasm_audio_smoke.sh` / `centered_text_smoke.sh` — each builds an example to
  wasm and runs it headless. See **`docs/WASM_SMOKES.md`**.
- **GPU render tests run on CI** (the `render` job): Mesa **lavapipe** (software Vulkan) renders
  `tests/render.rs` headlessly on the GPU-less ubuntu runner, asserting renderer-tolerant invariants
  (sprite/text/lighting/letterbox). `SKELETON_REQUIRE_GPU=1` = hard-fail on no adapter (else skips
  cleanly; runs under `verify.sh` where a GPU exists). See **`docs/RENDER_TESTING.md`**.

---

## Project direction (read `docs/VISION.md`)

This engine is a **skeleton**: a hackable, MIT-licensed, genre-agnostic 2D engine meant
to be forked and extended. Priorities — (1) open-source skeleton others can fork,
(2) personal foundation for 2D games, (3) learning vehicle.

When doing feature work, follow the core loop from `docs/VISION.md`:

- **A new feature is not done until a small, playable example game in `examples/`
  exercises it in real play.** The example is the acceptance test.
- **If the API feels awkward while writing that example, fix the API before release.**
- Keep new code fork-friendly: clear module boundaries and extension points. Breadth
  first, but not by leaving an unreadable mess.

---

## Module map

Where to read to find a given thing:

| Looking for | File |
|---------|------|
| Engine entry point, main loop, render orchestration, `load_image` (+ **`load_image_with_format`** = upload a texture with a caller-chosen `wgpu::TextureFormat`, e.g. `Rgba8Unorm` linear data textures sampled without the sRGB decode; default `load_image` stays `Rgba8UnormSrgb`; mirror `SpriteRenderer::load_texture_with_format`; example `texture_format`); **headless screenshot** — `App::save_screenshot_headless(frames, path)` / `screenshot_headless(frames) -> (w,h,RGBA8)` render the real render path into an offscreen texture with **NO window/surface/display** (works with the monitor off/asleep/locked; `GpuContext::new_headless` = surfaceless adapter + offscreen color + `read_headless_rgba`; renderer init factored into `App::init_gpu_renderers`, shared with the windowed path; native-only; example `headless_screenshot` + `scripts/headless_screenshot_smoke.sh`); **headless EDITOR screenshot** — `App::screenshot_editor_headless(frames, path)` / `screenshot_editor_headless_rgba` ALSO draw the **egui editor overlay** with NO window: the editor egui is normally driven by the windowed `egui_winit` loop (`begin_egui_frame` needs a window), so it never appears in a plain headless shot — these drive egui manually (synth `RawInput` → `App::update_editor_ui` → tessellate → the render path's egui pass composites it onto the offscreen texture), with a fresh `DebugUi`/egui-`Context` + an offscreen-format `egui_wgpu::Renderer`; lets editor UI (Inspector/EngineStats/the shortcuts cheatsheet) be **visually verified with no display** + on CI lavapipe (`tests/render.rs::editor_overlay_renders_headless`); `set_editor_shortcuts_visible(bool)` opens the cheatsheet, `editor_select_entity(e)` populates the Inspector; overlay capture leaves the editor mode `Off` (so it draws the unconditional cheatsheet/toasts, not the `Overlay`-gated EngineStats/Inspector windows); example `editor_headless_shot`. **DOCKED variant** — `App::screenshot_editor_docked_headless(frames, path)` / `screenshot_editor_docked_headless_rgba` enter `EditorMode::Docked` first so the **docked side panels** (toolbar / left entity list incl. the per-row eye toggle / right inspector / bottom Data Tables) AND the game scene composited into the central viewport render headlessly — unblocks visual verification + golden-image tests of docked-panel editor UI; the central viewport is a debounced offscreen RT (3-frame warm-up) so pass `frames>=5` for it to appear (fewer = side panels render, placeholder central); test `editor_docked_renders_headless`, example `editor_docked_headless_shot`) | `src/app.rs`, `src/app/headless.rs`, `src/renderer/context.rs` |
| Handle<T>, ImageAsset, ScriptAsset, AssetServer (asset load / caching / hot reload); **HotReloadable** trait + `App::register_hot_reloadable::<T>` (fork-friendly hot-reload extension point — built-in registries auto-registered; native-only); **`App::load_image_bytes(key, bytes)`** / `AssetServer::load_image_bytes` (register an in-memory / `include_bytes!` image under a VERBATIM logical `key` — NOT `asset_key`/canonicalized, so `key` = cache key = `Handle::path()` = render key, and `Sprite::with_handle(h)`/`textured(key)` render it exactly like a path load [the identity-must-agree invariant that dodges the 2026-05-29 white-sprite bug]; NO fs read + NOT pushed to `pending_textures` — GPU upload rides the per-frame `upload_asset_server_images_to_gpu` seam like async loads [decode via `decode_bytes_with_state` in `asset/image_loading.rs`]; **cross-platform incl. wasm** [`image` un-gated; single-file jam/wasm builds where async fetch is otherwise needed]; corrupt bytes report through `asset_failures()`; example `embedded_image`) | `src/asset.rs`, `src/app/assets.rs` |
| **Asset roots + loud failures** (`asset_path` — where a RELATIVE asset path is read from, and what happens when it's missing): every asset API is path-based and the path used to be read straight through `std::fs`, which resolves against the **process working directory** — so a packaged exe launched from anywhere but the repo root (double-click, shortcut, `cd / && game.exe`) failed EVERY texture load, got the magenta 1×1 fallback, and turned the whole window magenta with only a `warn!`. **`resolve(path)`** now resolves relative paths against an engine-determined root: candidates = macOS bundle `Contents/Resources` → exe dir → exe ancestors (`ANCESTOR_DEPTH`=4) → cwd → cwd ancestors, first candidate where the file EXISTS wins (absolute paths unchanged; wasm = identity, no fs). Exe-derived candidates precede cwd so a shipped build never picks up a stray `assets/`; a dev build's exe (`target/debug/examples/x`) reaches the repo root via ancestors, which is why **no existing example changed**. **`set_asset_root(p)`/`App::set_asset_root`** pins one explicit root (then it's the ONLY candidate). ⚠️ **`resolve` is applied ONLY at the ~10 fs-READ sites** (`renderer/texture.rs`, `asset/image_loading.rs`, `audio/playback.rs`, `scripting/loading.rs` + 7 RON loaders) — cache keys and `Handle::path()` stay the caller's LOGICAL string, because rewriting them re-breaks the 2026-05-29 "Unify Image Texture Cache Keys" bug (handle canonical vs GPU key relative → white sprite). **The hot-reload WATCHER does resolve, though** (v0.129.0): `watch_path` + the image watch register `notify` on `resolve(logical)` (the file that actually exists — a logical path is unwatchable in a packaged layout, so the watch used to silently fail) and record `asset_key(resolve(logical)) → asset_key(logical)` in an internal `watched_resolved_to_logical` map; `poll_reloads` translates each event back to the logical key (`logical_for_changed`, fallback = today's `asset_key` so dev-from-repo-root stays an identity map, byte-identical) BEFORE the `is_known` check, so images re-decode + RON registries reload by their logical key — F2 hot-reload now fires from a foreign cwd, closing EW-008's last acceptance clause. Identity/cache keys unchanged. Example `hot_reload_asset_root` + `scripts/hot_reload_smoke.sh` (real `notify` round-trip under a pinned root); pure-mapping unit tests in `asset/tests.rs`. **Loud failures**: `record_failure` logs `error!` (was `warn!`) with **the roots it searched**, dedups by path (one `load_image` feeds both AssetServer + the renderer), and feeds **`asset_failures()`/`App::asset_failures()`** (`AssetFailure{path,error}`); **`set_strict_assets(true)`/`App::set_strict_assets`** panics at the load instead of falling back. **EVERY path-based loader reports through it** (v0.127.0 — was images only): the 7 `App::load_*` RON wrappers (`data_table`/`animation_clips`/`particle_configs`/`dialogue`/`trigger_zones`/`zone_effects`/`anim_effects`, in `app/editor/loading.rs`, message names the registry key: `data table 'items': …`) + Rhai script read AND compile + audio file read. Rationale: a failed RON load used to `warn!` + register NOTHING, and an empty registry is indistinguishable from a legitimately empty one — a downstream packaged build booted with an empty shop + no dungeons and needed a PLAYER bug report to find (EW-007). ⚠️ Deliberately NOT failures: a **hot-reload** failure (file loaded once; a half-saved edit must not kill a strict dev build — still `warn!`) and a valid-but-empty asset. Example `packaged_assets` (real + MISSING texture and data table; `HEADLESS_SHOT` = an acceptance test that exits non-zero unless the missing table is REPORTED) + `scripts/packaged_assets_smoke.sh` (runs it from `/` — running it from the checkout is the case that always worked); regression test `tests/asset_root.rs` (its OWN integration binary — it moves the cwd, which is process-global, so **do not add a second test to that file**); loader tests in `app/editor/loading.rs` (key on a UNIQUE path — `asset_failures()` is process-global, never assert on its length) | `src/asset_path.rs` |
| TextureAtlas (uniform grid atlas), AtlasSprite (atlas tile render component) | `src/atlas.rs` |
| Reflect trait, ReflectValue (`F32`/`I32`/`Vec2`/`Bool`/`String`/`Color`, `#[non_exhaustive]`; runtime field read/write, egui Inspector integration); **`#[derive(Reflect)]`** proc-macro (in the `engine_reflect_derive` workspace crate, used via `engine_reflect_derive::Reflect`; path dev-dependency, not feature-gated) | `src/reflect.rs`, `engine_reflect_derive/` |
| DataTable, DataTableRegistry (schema-agnostic RON data tables; `App::load_data_table`, hot-reloaded, edited in the editor's Data Tables panel); `App::register_editable_component::<T>` (one-call reflect+clone+serde+inspector registration) | `src/data_table.rs`, `src/app/editor.rs` |
| **RonRegistry<V>**, **RonLoadable** (generic `name→value` RON registry with native canonical-path hot-reload; backs the particle/dialogue/animation-clip config registries AND is a **fork-friendly custom-asset registry** — a game impls `RonLoadable` for its own config type + uses `RonRegistry::load`/`get`/`names` w/o forking the engine; `load`/`reload_path` native-only [no wasm fs]; example `ron_registry`) | `src/ron_registry.rs` |
| ScriptAsset, ScriptRunner, ScriptingSystem, **ScriptRegistry** (Rhai scripting; `ScriptAsset`+loading live in `scripting/` not `asset.rs` — `ScriptRegistry` is the World resource that stores/loads/hot-reloads scripts, decoupled from `AssetServer`; `engine::ScriptAsset` re-exported at crate root) | `src/scripting.rs`, `src/scripting/{asset,loading,execution}.rs` |
| DebugUi (egui overlay, F1 toggle, custom panels via `ctx()`) | `src/debug_ui.rs` |
| In-game editor: F1 overlay + F2 **docked mode** (dock layout, viewport input gate, engine-level editor pause, gizmo (move + 8-handle resize + rotation handle) + screen-space UI gizmo, scene save, Data Tables panel, component copy/paste + entity-list search + **entity-list sort** (`docked/entity_kind.rs::sorted_entity_list` + `EntitySortMode` Insertion/Name/Kind toolbar toggle in the Entities tab: Default=raw order / A–Z=case-insensitive label / Type=grouped by `EntityKind` [the type-icon's classification] then name; display-only stable sort, so the world + scene-save order are untouched; 3 unit tests) + **per-row visibility toggle** (eye 👁/🙈 adds/removes `Hidden`, dims hidden rows) + **per-row type-icon** (`docked/entity_kind.rs::entity_type_icon` → shared `entity_kind` classifier [also backs the Kind sort, so icon + sort never drift]: a subtle glyph before each label hinting the entity's kind by its most salient component — light 💡 / tilemap 🗺 / particles ✨ / camera-rig 🎥 / animation 🎬 / UI 🔘 / sprite 🖼 / transform-only 🔹 / bare ·, first-match priority; shown in both the Entities list and the Scene tree; glyphs picked from egui's bundled emoji set so none render as □ tofu, verified via the headless docked capture; 6 unit tests) + **inline entity rename** (`src/app/editor/ui/rename.rs`: double-click an entity-list row **or a Scene-tree node** → a focused text box replaces the label; Enter/click-away commits `Tag(buffer)`, Esc cancels, blank/despawned cancels; in the Scene tree the box is NOT wrapped in the DnD drag source, so typing/dragging in it never starts a reparent; `App::editor_begin_rename` [public, drives it headlessly] + `editor_commit_rename`/`editor_cancel_rename`; transient `EditorState::entity_rename`/`EntityRename`, not serde; render tests `editor_docked_inline_rename_renders_headless` + `editor_docked_scene_tree_rename_renders_headless`) + **row/node context menu** (right-click an Entities-list row **or a Scene-tree node** → Rename / Duplicate / Focus camera / Delete, **plus a Scene-tree-only ＋ Add child** = spawn a fresh `Transform`+`Tag` entity parented under the node via cycle-safe `hierarchy::reparent`, then select the child; the shared `docked/context_menu.rs::entity_context_menu` records `(entity, EntityContextAction)` and `App::editor_apply_entity_context_action` selects that entity first [so it acts on the right-clicked entity, not the prior selection] then runs the existing toolbar/shortcut op — `AddChild` selects the new child instead; 6 unit tests) + **Scene-tab drag-to-reparent** (`src/app/editor/ui/reparent.rs` + `docked/scene_tab.rs::scene_tab_body`: egui DnD — drag a tree node onto another to re-parent it, onto the bottom "⤴ unparent" zone to detach to a root; edits go through `App::editor_reparent` [public, headless-drivable] → engine **`hierarchy::reparent`** [`src/hierarchy.rs`, public, re-exported] = cycle-safe `attach`+`detach` that no-ops a self/descendant-cycle/no-change move [unlike low-level `attach`]; `App::editor_show_scene_tree` opens the Scene tab for headless capture; **undoable** — a successful reparent pushes `EditorCmd::Reparent{entity,old_parent,new_parent}` so Ctrl+Z restores the old parent / Ctrl+Shift+Z re-applies, both via `hierarchy::reparent`; a rejected drag records nothing; render test `editor_docked_scene_tree_reparent_renders_headless`), **keyboard shortcuts** (`src/app/editor/ui/shortcuts.rs`: Ctrl+Z/Shift+Z undo/redo, Ctrl+C/V copy/paste, **Ctrl+S** save scene, **Ctrl+D** duplicate selection, **Delete** delete selection, **F** focus camera on selection, **?** toggle the cheatsheet — bare keys gated on `egui_wants_keyboard_input` so typing in a field doesn't fire them; selection-scoped ops factored into `App::editor_{delete,duplicate}_selection`/`editor_focus_camera_on_selection`) + a **`? Keys` shortcuts cheatsheet** window (`show_shortcuts`, toolbar `? Keys` button or `?`), **action toasts** (`src/app/editor/ui/toasts.rs`: transient colour-coded bottom-right feedback popups — `App::editor_toast`/`editor_toast_success`/`editor_toast_error` [public] + internal `push_editor_toast`; auto-expire+fade via `EditorState::toasts`/`Toast`/`ToastKind`, drawn by `draw_editor_toasts` in both modes; wired to delete/duplicate/paste/save + prefab save/spawn [success/error]), prefab save/spawn, world-aligned grid overlay + cursor readout, bounds/colliders debug overlay, pathfinding-grid overlay (per-`Tilemap` walkable/blocked cells), audio bus mixer panel (per-bus volume sliders), particle live-tuner (inspector `ParticleEmitter` field drags + reset), lighting editor (`PointLight` add/edit + global `AmbientLight` control), state-machine editor (list-based `AnimationStateMachine` graph: states/transitions/params + edits), timeline editor (`Timeline` per-track keyframe list + playback controls), settings persistence (RON config), **Tile Paint** = viewport tile-painting for selected `Tilemap` entities — brush(N×N)/rectangle/bucket tools + eyedropper(Alt+click), L-paint/R-erase/digit-pick, **image-swatch palette** (atlas thumbnails via egui `register_native_texture`), stroke-level undo, visual-only/native; example `tile_paint`; **localization** — Korean-by-default editor via `i18n::tr(en, ko)` (English = source of truth, Korean inline) + `EditorLocale` (RON-persisted, EN/한국어 toolbar toggle); CJK renders via the **bundled Noto Sans KR (Hangul-only subset, ~2.3 MB — keeps crate < crates.io 10 MB limit; regen via `scripts/subset_korean_font.sh`, OFL in `assets/fonts/NotoSansKR-OFL.txt`)** installed as an egui fallback in `DebugUi::new_with_ctx` (`install_korean_fallback`, fixes □ tofu — also covers Korean data-table/tag values)); the docked UI lives in **`docked/{mod,toolbar,entity_kind,context_menu,entities_tab,scene_tab,inspector_tab,assets_tab,save_load}.rs`** (the 1619-line `docked.rs` split by concern, v0.106.1, behavior-preserving) | `src/app/editor/` (+ `i18n.rs`) |
| Full public API re-export list | `src/lib.rs` |
| Entity / Component / Resource / Query (`query`/`query2`/`query3`/`query4`/`query_opt2`; **mutable** `query_mut`/`query2_mut`/`query3_mut` — multi-component `*_mut` use `HashMap::get_disjoint_mut`, distinct types, no collect-then-`get_mut`); (+ `register_persistent` survives scene reset, via `App`). `world.rs` keeps the data model (Entity/Archetype/World structs + private archetype plumbing); the `impl World` surface is split by concern into `world/{entities,components,queries,resources,reflect,change_tracking,clone}.rs` (+ native-only `parallel.rs`), each re-opening `impl World` as a descendant module | `src/ecs/world.rs` (+ `src/ecs/world/`), `src/app.rs` |
| Event bus (`Events<E>`) | `src/ecs/events.rs` |
| `System` trait | `src/ecs/system.rs` |
| Scene transitions (Scene, SceneCmd, SceneChange; `App::set_scene` = Replace, **`App::push_scene`/`pop_scene`** = stack overlays e.g. pause menu), SystemRegistrar (labeled system registration from `on_enter`) | `src/scene.rs` |
| **SceneTransition** (styled scene transition — fade/wipe/iris + AUTO scene-swap; the successor to the solid-colour `FadeTransition`): **`TransitionStyle`** (`#[non_exhaustive]` Fade / WipeLeft/Right/Down/Up / IrisIn / IrisOut) + `coverage` 0→1 (cover) →0 (reveal) across `phase` Out/In/Done, `speed`/`color`; ctor `new(style, half_duration)` + `with_color`. **`App::transition_to_scene(scene, style, half_duration)`** (from `&mut App`) / **`start_scene_transition(world, scene, style, half_duration)`** (the world-level twin — callable from a **system**) cover the screen, swap the scene WHILE HIDDEN (a `pub(crate) PendingSceneTransition` resource consumed at full cover), then reveal it; the `SceneTransition` resource is auto-registered **persistent** so the reveal survives the mid-transition world reset, and is dropped when done. Native-only render (`TransitionRenderer` fullscreen-quad pass, run in the fade slot in `frame.rs`; all-`vec4` uniform to dodge the WGSL vec3-align trap; `covered_at(x,y,aspect)` = CPU mirror of the shader for logic/tests); the swap still fires on wasm (no overlay). Insert a bare `SceneTransition` for a swap-less cover/reveal. Example `scene_transition` (keys 1-7 transition between 4 full-screen-colour levels, one per style; auto-swap integration test drives `App::update` headlessly) | `src/scene_transition.rs`, `src/renderer/transition.rs` |
| Transform, Sprite; **SpriteFlip** (`{x,y}` bool component — mirrors a sprite across X/Y at render time via UV-region flip, so it works on plain `Sprite` / `AtlasSprite` / `ShaderMaterial` + animation frames + atlas tiles, no texture swap or negated scale; absent/`default()` = byte-identical; ctors `horizontal()`/`vertical()`/`NONE` + `is_flipped()`; backed by `UvRect::flipped(x,y)` in `renderer/uv.rs`; applied in `renderer/sprite/collect.rs`, **excludes `NineSlice`**; clone+editor-add/remove registered like `RenderLayer`; example `sprite_flip`); **Hidden** (marker component — when present, the sprite pass [`renderer/sprite/collect.rs`, all 3 loops: Sprite incl. `NineSlice` / `AtlasSprite` / `ShaderMaterial`] skips the entity, so its sprite-family rendering is suppressed [does NOT affect particles/lights/text]; the engine side of the editor's per-entity **visibility (eye 👁/🙈) toggle** in the entity list + a game hiding entities without despawning; absent = byte-identical; clone+editor-add/remove registered like `SpriteFlip`; render test `hidden_component_suppresses_sprite`) | `src/components.rs`, `src/renderer/uv.rs`, `src/renderer/sprite/collect.rs` |
| WindowConfig, GameState, ShouldQuit, DebugDraw (filled rects via `rect_filled_z`; `DebugShape` is `#[non_exhaustive]`); **WindowOptions** (opt-in resource, `ImeConfig`-style — absent = default resizable window, so NO `WindowConfig` field added/breaking: `resizable` / `mode: WindowMode` (Windowed/**BorderlessFullscreen**) / `lock_aspect: Option<f32>` (live resize corrected back to ratio, native-only — `window.rs` `Resized` re-derives height from width, converges in one step); example `window_mode`); **DesignResolution** (opt-in fixed virtual canvas, e.g. 1280×720: `ViewportSize`=design size, all `DrawRect`/`DrawText`/sprites/particles authored in design space + scaled to the window with a **uniform centered letterbox**; cursor + `screen_to_world` mapped back to design space; ignored in docked editor; example `design_resolution`) + **Letterbox** (the computed transform, inserted every frame, identity when no `DesignResolution`: `clip_scale` (centered = pure clip scale, post-multiplied on each scene projection via `camera::apply_letterbox`) + `px_scale`/`px_offset` (logical-px scale+offset for text/cursor); OFF-path byte-identical); **TimeScale** (global `dt` multiplier for scene systems → hit-stop/slow-mo, `App::set_time_scale`) + **RealDt** (real unscaled per-frame dt, for systems that must opt out of time-scaling) + **FrameConfig** (`max_dt` per-frame delta cap — the main loop clamps each frame's `dt` so a stall can't tunnel physics/leap animations; default `0.1` s = the old hardcoded cap, auto-inserted; example `frame_dt_cap`) | `src/resources/` (split by concern: `debug_draw`/`display`/`fonts`/`lifecycle`/`profiling`/`render`/`time`, re-exported from `mod.rs`) |
| Camera (coordinate transforms, zoom; `screen_to_world`/`world_to_screen`; `bounds` + `clamp_to_bounds` world-bounds clamp, auto-applied by App after follow); **motion lookahead** (`lookahead` world-units field + `lookahead_speed` [`DEFAULT_LOOKAHEAD_SPEED`=3.0] — `Camera::update` derives the follow target's velocity from the follow-position delta, eases an offset toward `dir*lookahead`, and aims at `pos+offset` so the camera leads where the target is heading + recenters when it stops; `lookahead=0` default = byte-identical direct follow; read the smoothed `lookahead_offset()`; all internal to `Camera`, no schedule change; example `camera_lookahead`) | `src/camera.rs` |
| ParallaxLayer (`factor: Vec2` depth scroll: 1=world-locked, 0=screen-locked, >1=foreground; lazy base capture), ParallaxSystem (user-added: `pos = base + (cam - cam_ref) * (1 - factor)`; reads `Camera`, add after camera-mover systems; example `parallax_scroll`) | `src/parallax.rs` |
| **YSort** (top-down depth sort: marker component `{bias}` + **YSortSystem** [user-added, like `ParallaxSystem`] sets `Transform.z = position.y + bias` each frame so a sprite lower on screen [larger Y, Y increases downward] draws in front; sorts **within** a `RenderLayer`; `bias` = +half sprite height to sort by the feet; `query2_mut::<YSort,Transform>`; clone+editor-add/remove registered; example `ysort` [overlapping trees + a player weaving in front/behind, Space toggles sort on/off]) | `src/ysort.rs` |
| **HitFlash** (game-feel hit-flash: transient component `{color, secs}` + private `elapsed`/`base` runtime state + **HitFlashSystem** [user-added, like `YSortSystem`] `query2_mut::<HitFlash,Sprite>` snaps `Sprite.color` to `color` then `Lerp`s it back to the **pre-flash color captured on the first run** [not at construction] over `secs`, and **removes the component** when finished [restoring the original color exactly]; ctors `white(secs)`/`new(color,secs)` + read accessors `progress()`/`is_finished()`; `secs<=0` restores+removes same frame; pairs with a damage `ZoneEvent`/`CollisionEvent`/animation event by adding a `HitFlash` to the hit entity; **no serde** [transient effect, like `TriggerZone`] — clone+editor-add/remove registered like `YSort`; example `hit_flash` [Space flashes 3 targets white, fades back; headless auto-re-flashes staggered]) | `src/hit_flash.rs` |
| **FloatingText** (game-feel floating combat text: transient component `{text, color, velocity, size, lifetime, fade}` + private `elapsed` + **FloatingTextSystem** [user-added, like `HitFlashSystem`] ages each text, drifts it by `velocity` [default rises = −Y], fades alpha over `lifetime`, draws via `TextQueue` + `Camera::world_to_screen` [screen-space fallback if no camera], and **despawns the whole entity** when done — so put it on a dedicated ephemeral entity, NOT one you keep; ctors `new(text)`/`colored(text,color)` + `with_velocity`/`with_size`/`with_lifetime`/`with_fade`/**`with_z`** (`z: Option<f32>` UI-layer depth passed through to the queued `DrawText` — `Some(z)` composites the number among the UI rects so a higher-z overlay/pause-scrim COVERS it, `None` default = historical on-top pass, byte-identical; EW-004; render test `floating_text_with_z_hides_under_a_higher_z_rect`), accessors `progress()`/`is_finished()`; free `spawn_floating_text(world,pos,ft)` [usable from a system] + `App::spawn_floating_text{,_colored}`; **no serde** [transient, like `HitFlash`] — clone-registered but NOT editor-add [it despawns its own entity]; pairs with a damage event; example `floating_text` [Space pops numbers over 3 targets, click pops at cursor, P toggles a z-100 scrim the layered floats hide behind, Z switches layered/on-top, headless auto-fires]) | `src/floating_text.rs` |
| **SpriteTrail** (game-feel motion trail/afterimage: `SpriteTrail` component {interval, lifetime, start_alpha}+private timer on a moving `Sprite`+`Transform`, `SpriteTrailGhost` [emitted fading copy], + **SpriteTrailSystem** [user-added, like `HitFlashSystem`] every `interval`s snapshots the sprite into a short-lived ghost drawn just BEHIND it [`z-=0.1`; full `Sprite` clone so textured+colored both work], then fades each ghost's alpha→0 over `lifetime` + despawns it; ONE ghost/source/frame [no slow-frame storm], ghosts carry no `SpriteTrail` so never emit [no ghost-of-ghost]; a ghost fades only ITSELF [never touches the source → safe on a kept entity, unlike a self-despawning effect]; ctors `new(interval,lifetime)`/`default` [`DEFAULT_TRAIL_INTERVAL`=0.045/`_LIFETIME`=0.4/`_START_ALPHA`=0.55] + `with_start_alpha`/`with_lifetime`; clone+editor-add/remove registered like `YSort`; example `sprite_trail` [orbiting box leaves a fading arc, Space toggles; 6 unit tests]) | `src/sprite_trail.rs` |
| InputState, InputMap (keyboard + gamepad bindings: `bind_gamepad_button`/`bind_gamepad_axis` + `AxisBinding`, `*_with_gamepad` resolution) | `src/input/` |
| **InputBuffer** (game-feel jump forgiveness: a pure-logic helper [driven by the game like `Timer`/`Tween` — NOT a system/component] combining **input buffering** [a jump pressed just BEFORE landing still fires on touchdown] + **coyote time** [a jump pressed just AFTER leaving a ledge still fires]; per-frame `set_grounded(bool)` → `press()` → `try_consume()->bool` → `tick(dt)` **last** [ticking last means a 0-length window disables forgiveness yet a grounded press still jumps — no footgun]; `since_press<=buffer_secs` = live press, `grounded||coyote>0` = eligible, consume clears both so one press = one jump [no air double-jump]; ctors `new(buffer_secs,coyote_secs)`/`default()` [`DEFAULT_BUFFER_SECS`=0.12/`DEFAULT_COYOTE_SECS`=0.10], accessors `buffered_remaining`/`coyote_remaining`/`is_grounded`; genre-agnostic [any buffered action gated by a forgiving condition]; example `input_buffer` [kinematic box on a platform w/ a gap — walk off for coyote, tap early for buffer; 9 unit tests]) | `src/input_buffer.rs` |
| GamepadState, GamepadButton, GamepadAxis (gilrs-fed on Windows/Linux; **macOS uses a GameController-framework backend** — `src/input/gamepad_macos.rs`, polled from `about_to_wait`, since gilrs/IOKit-HID can't read modern Xbox/PS5 pads there; gilrs left uninit on macOS; diagnostic: example `gamepad_probe`) | `src/input/gamepad.rs`, `src/input/gamepad_macos.rs` |
| PhysicsWorld, PhysicsBody, PhysicsSystem (syncs body position **and rotation** → Transform), CollisionEvent, BodyHandle/ColliderHandle (opaque newtypes; `.raw()` = rapier escape hatch); **solver tuning**: `set_solver_iterations(n)` (rapier default 4 — raise for stiff joints/ragdolls/high mass ratios) / `with_integration_params(IntegrationParameters)` builder / `integration_params()` getter; example `solver_iterations` (heavy-ended joint chains, 2 vs 16 iters) | `src/physics/` |
| CharacterController (+ `request_drop`/`is_dropping` for one-way; **`one_way_tolerance`**/`with_one_way_tolerance` = one-way landing skin width in physics units, default `DEFAULT_ONE_WAY_TOLERANCE`=0.05 [suits PPU≈64] — scale it to your PPU; example `one_way_tolerance`), RaycastHit, cast_ray, cast_ray_with_normal, move_character | `src/physics/character.rs`, `src/physics/world.rs` |
| add_kinematic_box, add_kinematic_circle, add_static_from_tilemap (one-shot), sync_static_from_tilemap + TileColliderIndex (incremental — diff add/remove for runtime-mutated tilemaps), **TilemapColliders** component + SolidTiles + `sync_tilemap_entity_colliders`/`App::sync_tilemap_colliders` (opt-in: keep tile colliders synced when the editor's Tile Paint or a game's `set_tile` mutates the map; example `dig_quest`), TileCollider, set_one_way/is_one_way | `src/physics/world.rs` |
| add_revolute_joint, add_distance_joint, add_prismatic_joint, remove_joint (return/take engine `JointHandle` newtype wrapping rapier; example: `crane_wrecking_ball`) | `src/physics/world/joints.rs` |
| SpatialGrid, Collider, CollisionLayer (SpatialGrid is mirrored to a World resource by CollisionGridSystem) | `src/collision/` |
| **TriggerZone** (Area2D-style enter/stay/exit zones: component `{shape: TriggerShape Circle/Rect, mask: CollisionLayer, occupants: Vec<Entity>}` + **TriggerZoneSystem** [user-added, like `YSortSystem`; add AFTER `CollisionGridSystem`] reads the mirrored `SpatialGrid`, queries each zone's region [`query_radius`/`query_aabb`] for layer-matching entities, diffs vs last frame's `occupants`, and sends **`ZoneEvent`** `Entered`/`Stayed`/`Exited{zone,other}` on `Events<ZoneEvent>` [register via `App::register_event`; dropped+one-time-warn if unregistered, but `occupants` still updates for direct polling via `contains()`]; watched entities need `Transform`+`Collider`[+`CollisionLayer`]; a zone never reports itself; **name `ZoneEvent` is deliberately distinct from physics `TriggerEvent`** [rapier sensors]; ctors `circle(r)`/`rect(half)` + `with_mask`; clone+editor-add/remove registered like `YSort`; example `trigger_zones` [player walks through heal/damage/goal zones — events drive an entry counter, occupant-polling tints each zone]; **data-driven (RON)** — `TriggerZoneSet`/`TriggerZoneRegistry`/`TriggerZoneSetError` load zones from a RON file (private serde-mirror types like `ParticleConfigSet`, NO serde on the runtime `TriggerShape`/`CollisionLayer`; `App::load_trigger_zones(name,path)` = registry + native hot-reload [auto-registered like the particle/dialogue registries], `App::spawn_trigger_zones(name)` → one entity per def with `Transform`+`TriggerZone`+`Tag`[name]; def = `(tag, pos, shape: Circle/Rect, mask)`; zones carry no `Sprite` — the game draws them; example `data_trigger_zones`)) | `src/trigger_zone.rs` |
| **Zone→Effect bindings** (data-driven reactions to `ZoneEvent`s — author *what happens* on a zone in RON, the companion to data-driven `TriggerZone`s' *where*: `Effect` enum `SpawnParticles{particles,count,at:EffectAnchor Other/Zone,offset:(x,y)}` [one-shot burst from a named `ParticleConfigRegistry` emitter, continuous emission forced off; **`offset`** = world-space `(x,y)` displacement added to the anchor's `Transform` position, default `(0,0)` byte-identical — e.g. footstep dust at the feet not the center; shared by both effect sources] / `PlayTone{freq,dur,vol,bus}` [`Audio` facade] / `Flash{color:(r,g,b,a),secs}` [`HitFlash` on the entrant, skipped if no `Sprite`], each gated by `ZonePhase` Entered[default]/Stayed/Exited in `ZoneEffectRule{on,effect}`; `ZoneEffectBindings{bindings: HashMap<tag,Vec<rule>>}`+`from_ron_str`+`rules_for`; `ZoneEffectRegistry`+`RonLoadable`/`HotReloadable` auto-registered in `App::new`; `App::load_zone_effects(name,path)` mirrors `load_trigger_zones`; **`ZoneEffectSystem::new(name)`** [user-added, add AFTER `TriggerZoneSystem`] reads `Events<ZoneEvent>` → resolves the zone's `Tag`→key → collect-then-apply [spawn burst / add `HitFlash` / play tone]; example `zone_effects` [heal/damage/goal zones + particle emitters + reactions all authored in 3 RON files, composed by tag]. The shared `Effect`/`EffectAnchor` vocabulary + `pub(crate)` apply machinery [`resolve_effect`/`apply_pending`/`lookup_emitter`] live in **`src/effect.rs`**, reused by `anim_effect`) | `src/zone_effect.rs`, `src/effect.rs` |
| **Animation→Effect bindings** (the `anim_effect` companion to Zone→Effect — fire RON `Effect`s on a tagged animation frame: `AnimEffectBindings{bindings: HashMap<tag, Vec<Effect>>}`+`from_ron_str`+`effects_for` [bare `Vec<Effect>`, **NO phase** — an `AnimationEvent` is an instantaneous fire], `AnimEffectRegistry`+`RonLoadable`/`HotReloadable` auto-registered in `App::new`, `App::load_anim_effects(name,path)`; **`AnimEffectSystem::new(name)`** [user-added, add AFTER `AnimationSystem`] reads `Events<AnimationEvent>` → keys on `AnimationEvent.tag` → applies via the shared `crate::effect` machinery, anchoring `SpawnParticles` + `Flash` at the animating entity [`EffectAnchor` not consulted; the shared `SpawnParticles.offset` still applies — `anim_effects` spawns the footstep dust at the feet]; example `anim_effects` [walk cycle's contact frames fire a dust burst at the feet + click tone + warm flash, all in RON]) | `src/anim_effect.rs` |
| BehaviorTree, BehaviorNode, Sequence, Selector, Inverter, AlwaysSucceed, BehaviorSystem, Blackboard, BlackboardValue (`Path` variant + `set_path`/`get_path`) | `src/behavior.rs` |
| Seek, Flee, Arrive, Wander, SteeringVelocity, SteeringSystem (steering behaviors; O(1) per-entity component lookup; `Wander::direction_fn`/`with_direction_fn` overrides the direction picker — swap in real RNG without forking the system) | `src/steering.rs` |
| PathGrid, find_path (4-dir), find_path_diagonal (8-dir A*, octile heuristic, no corner-cut), PathGrid::from_tilemap; **MAX_PATH_GRID_CELLS** (pub cell-count cap; a grid exceeding it — or whose dims overflow `i32` — is created EMPTY + logs `error!` instead of silently failing) | `src/pathfinding.rs` |
| **FovMap** (grid field-of-view / fog-of-war — a plain owned helper like `PathGrid`/`InputBuffer`, NOT an ECS component/system; the game keeps one + recomputes on move): opaque/visible/**revealed** row-major grids + `compute(origin, radius)` = **recursive shadowcasting** over 8 octants (opaque cell is lit then shadows everything behind; Euclidean `dx²+dy²≤radius²`; clears `visible` each call, accumulates `revealed` = explored memory), `from_path_grid` (non-walkable cell → opaque, coords 1:1), `set_opaque`/`is_opaque`/`is_visible`/`is_revealed`/`clear_visible`/`reset`, plus **`line_of_sight(a,b)`** = point-to-point Bresenham sight check (endpoints excluded — stealth guard sight line). `new` caps cells at `MAX_PATH_GRID_CELLS` (over-cap/overflow → empty + `error!`, like PathGrid). Genre-agnostic (roguelike FOV / stealth vision / fog-of-war); example `fov` (top-down dungeon — walls cast shadows, explored dims, unseen black, gems revealed only in sight); also composed with procgen in the **`roguelike`** capstone (`FovMap::from_path_grid(&DungeonMap::to_path_grid())`) | `src/fov.rs` |
| **Procedural map generation** (three deterministic generators over a shared `DungeonMap` grid — a plain owned grid like `FovMap`/`PathGrid`, not an ECS type: **BSP rooms + corridors**, **cellular-automata caves**, and **perfect mazes**): **`generate_bsp_dungeon(w, h, seed: u64, &DungeonParams)`** recursively splits the map into partitions, carves a room per leaf, and connects sibling partitions with L-corridors while unwinding — so the dungeon is **guaranteed connected** (every room reachable). **Deterministic** (same seed + params → identical `DungeonMap`, so a game stores just the seed); seeded by the shared **`engine::Rng`** (SplitMix64 — mapgen adopted it and dropped its own private PRNG; see the `rng` row). `DungeonMap{width,height,rooms:Vec<Room>}` (+ `Tile` Wall/Floor) — `tile`/`is_floor`/`is_wall` (OOB = Wall), `first_room_center`, `to_tilemap_tiles(floor_id, wall_id)` → `Vec<Vec<u32>>` to build a `Tilemap`, **`to_path_grid()` → `PathGrid`** (walkable = floor; the direct bridge to `find_path` enemy pathfinding AND — via `FovMap::from_path_grid` — to field-of-view, one line each, same layout); `Room{x,y,w,h}`+`center`/`contains`; `DungeonParams` min/max leaf + min_room + margin + max_depth. Cell count capped at `MAX_PATH_GRID_CELLS`. **Second generator — `generate_cellular_cave(w, h, seed, &CaveParams)`**: an **organic cave** via random-rock fill → cellular-automata smoothing (the classic 4-5 birth/survival rule — an existing wall survives with one fewer neighbor) → keep only the **largest connected cavern** (so caves are **guaranteed connected** too); returns the same `DungeonMap` (so `to_path_grid`/`to_tilemap_tiles`/`FovMap::from_path_grid` all compose), recording a single 1×1 `Room` at the cavern's central cell so `first_room_center` is a valid spawn. `CaveParams{initial_wall_prob, steps, wall_threshold}` (defaults 0.45/4/5). **Third generator — `generate_maze(w, h, seed, &MazeParams)`**: a **perfect maze** via a recursive-backtracker (DFS, explicit stack — a maze nests as deep as every cell) walk over the odd-coordinate *junction* cells, knocking out the wall between each junction and a random unvisited neighbor → a **spanning tree** over the junctions, so it's **guaranteed connected by construction** (no keep-largest pass) and, for `braid_chance == 0.0`, exactly one path joins any two cells; returns the same `DungeonMap` (records a single 1×1 `Room` at the start junction `(1,1)`; a pure maze has exactly `2·jw·jh − 1` floor cells = J junctions + J−1 passages, the spanning-tree count). `MazeParams{braid_chance}` (default 0.0) optionally *braids* afterward — reopening a fraction of dead-end walls into loops; braiding only removes walls so connectivity holds at any value. Genre-agnostic (roguelike / replayability); all three compose with `FovMap`. Examples `procgen_dungeon` (renders the full dungeon, rooms tinted warmer than corridors, WASD walk, R regenerates a new connected dungeon), `cave_generation` (organic cave, floor-touching-rock cells tinted darker to show the cavern outline, WASD walk, R regenerates a fresh connected cave; headless self-checks single-cavern connectivity), `maze_generation` (perfect maze, dead-end tips tinted warm, WASD walk, R regenerates, B toggles braiding perfect↔braided; headless self-checks single-region connectivity) + **`roguelike`** (the procgen↔FOV capstone — a seeded BSP dungeon explored under fog-of-war via `FovMap::from_path_grid(&map.to_path_grid())`, a gem hidden in every non-spawn room, R descends to a fresh dungeon) | `src/mapgen.rs` |
| **`Rng`** (seedable deterministic SplitMix64 PRNG — a plain owned helper like `PathGrid`/`FovMap`, NOT an ECS resource; `new(seed)` → same seed = same stream, so a game stores just a seed to reproduce a run/level/loot): `next_u64`/`next_u32` + `range(lo,hi)` i32 `[lo,hi)` (empty range → `lo`, no `% 0` panic) / `f32_unit()` `[0,1)` / `range_f32` / `bool()` / `chance(p)` / `pick(&[T])` / `shuffle(&mut [T])` (Fisher–Yates); a golden-value test locks the stream so the SplitMix64 constants can't silently drift (which would change every seed-derived dungeon/loot). **`WeightedTable<T>`** (weighted random selection — the loot-drop / spawn-table primitive: `new().with(item, weight)` / `add` [f32 relative weights, `≤0` or non-finite ignored], `pick(&mut Rng)` / `pick_index` / `total_weight` / `len` / `items`; draws are driven by a passed `&mut Rng`, so a table + a seed reproduce the same sequence). `mapgen` seeds its BSP from `Rng` (dropped its private copy). Genre-agnostic (procgen / loot / shuffles / reproducible runs). Example `loot_table` (weighted rarity drops + a live histogram converging to per-rarity target markers, Space/A draw, R replays the identical sequence, N new seed) | `src/rng.rs` |
| AnimationPlayer, AnimationClip, AnimationSystem, BlendWeight (crossfade = true 2-UV shader-lerp; renderer `mix`es from/to frames); **AnimationEvents** (frame-triggered events: per-entity `Vec<FrameEvent{clip,frame,tag}>` component beside `AnimationPlayer` — NOT a clip field, so additive/non-breaking; when `AnimationSystem` advances the playhead **onto** a listed frame it sends `AnimationEvent{entity,clip,frame,tag}` on `Events<AnimationEvent>` [game calls `App::register_event::<AnimationEvent>()`; dropped+one-time-warn if unregistered]; fires on frame *transitions* only — initial frame at spawn never fires, a loop re-fires frame 0 on each wrap; outgoing clip only during crossfade; builder `AnimationEvents::new().on(clip,frame,tag)`; clone+editor-add/remove registered like `RenderLayer`; example `animation_events` [walk cycle, footsteps on contact frames 1/3]) | `src/animation/player.rs`, `src/animation/system.rs`, `src/animation/events.rs` |
| AnimationClipSet, AnimationClipRegistry, ClipSetError (data-driven animation: named clips loaded from RON `(atlas, clips)`, frame indices → UvRect; `App::load_animation_clips` = registry + DataTable-style hot-reload) | `src/animation/clip_set.rs` |
| UvRect, BlendUv (GPU UV-region types, consumed engine-wide) | `src/renderer/uv.rs` |
| AnimationStateMachine, StateMachineSystem, TransitionCond, AnimParam (per-transition crossfade via `add_transition_crossfade`; editor **State Machine panel** — `state_names`/`state`/`param` accessors + `set_current_state`/`set_state_clip`/`remove_state`/`remove_transition`/`set_transition_conditions`/`set_transition_crossfade` edit ops drive a list-based graph editor in the docked inspector — live param editing + add-transition + condition add/remove; example `sm_crossfade`) | `src/animation/state_machine.rs` |
| BlendTree1D, BlendEntry, BlendTreeSystem (1D parameter-driven auto transitions + crossfade) | `src/animation/blend_tree.rs`, `src/animation/blend_system.rs` |
| SkeletalAnimator, SkeletalClip, BoneTrack, BoneKeyframe, SkeletalAnimationSystem, SkeletonBuilder (2D cutout skeletal animation) | `src/skeletal.rs` (details: `docs/SKELETAL.md`) |
| UI (UiNode [+ `with_visible` builder — spawn a widget hidden, e.g. pause-menu entities revealed later], Button [EW-005: `with_colors(normal,hovered,pressed)`/`with_disabled_color`/`with_text_color`/`with_font_size`/**`with_corner_radius`** builders + `corner_radius` field — SDF-rounded bg rect like the newer widgets, `0.0` default = sharp/byte-identical; demo in `ui_rounded`], Label, TextInput, ScrollView, Panel, LayoutSystem, UiEvent [now `PartialEq`]); **keyboard focus** — `UiFocus` resource (`Option<Entity>`, auto-inserted) + `UiSystem`'s focus pass (`src/ui/system/focus_pass.rs`): **Tab/Shift+Tab** (or gamepad **D-pad Down/Up** / **left stick** Down/Up) cycle focus across focusable widgets (Button/TextInput/Slider/CheckBox, by entity index, skips hidden/disabled), draws a focus ring (styled by the **`FocusRingStyle`** resource — `color`/`thickness`/`enabled` + optional `pulse_hz`/`pulse_min_alpha` ("breathing" alpha pulse, `pulse_hz=0` default = steady ring; clock accumulated in `UiSystem`) + optional **`corner_radius`** (rounds the ring; `0.0` default = sharp 4-bar, byte-identical), auto-inserted with the default amber 3px ring; `enabled=false` or `thickness<=0` suppresses it for a game's own indicator), **Enter/Space** (or gamepad **A**/South) activate (button click / checkbox toggle), **←/→** (or D-pad/left-stick Left/Right) nudge a focused Slider, click also focuses (by z-order — see pointer capture); **pointer capture** — a shared per-frame occlusion map (`src/ui/system/capture.rs`, internal `PointerCapture`, rebuilt once by `UiSystem`) means a widget covered by a higher-z widget kind (e.g. a `Panel`, which blocks at its drawn `z-0.01` background) does NOT receive clicks/hover/scroll/click-to-focus through it — the topmost pointer-opaque surface absorbs the interaction; ties broken by z then entity index, `Label` excluded (not pointer-opaque); gamepad nav is folded into `InputSnapshot` from the first connected `GamepadState` pad (`src/ui/system/state.rs`, optional — no pad = no-op; **D-pad + left analog stick**, the stick edge-detected via `StickNav` hysteresis so one push = one step, no auto-repeat; **`StickNavConfig`** resource (opt-in, `activate`/`release` thresholds, default `DEFAULT_STICK_ACTIVATE`=0.6 / `DEFAULT_STICK_RELEASE`=0.35 [the old hardcoded literals] — `resolved()` clamps so `release<=activate`; retune the deadzone without forking; example `ui_nav_deadzone`)); **`DrawRect`** gains `corner_radius` + `border` (`with_corner_radius`/`with_border`; filled rounded rect or inset outline ring) via a dedicated UI SDF pipeline (`src/renderer/shaders/ui.wgsl` + `UiInstanceRaw`; `0,0` = fast path identical to before, sprite pipeline untouched); examples `ui_focus`/`ui_rounded`/`ui_nav_deadzone`; **game-feel capstone example `game_feel`** — one playable arena composing InputBuffer + SpriteTrail + HitFlash + FloatingText + Camera::shake + TimeScale hit-stop with an Esc pause/settings menu (TabBar/RadioGroup/Slider/Dropdown) whose settings drive the effects LIVE; **also ships to the web** (`examples/game_feel/web/` wasm-bindgen harness + render smoke `scripts/game_feel_web_smoke.sh`, shared `build_app()` dual-target entry); `UiSystem`/`SteeringSystem` hold reused scratch buffers → construct via `::default()`/`::new()` | `src/ui/` |
| Slider (horizontal slider; **`keyboard_step: Option<f32>`** + `with_keyboard_step` / `resolved_keyboard_step()` = per-slider absolute ←/→ nudge step, `None` = `DEFAULT_SLIDER_STEP_FRAC`=0.05 × range [the old hardcoded fixed step] — set an absolute step for discrete-level sliders; example `slider_keyboard_step`), CheckBox (toggle checkbox), **ProgressBar** (read-only UI gauge — health/mana/loading/XP: `{value: 0..=1, fill_color, bg_color, corner_radius, border, border_color}`, NON-interactive [the game drives `value` each frame, unlike `Slider`]; `system/progress_bar_pass.rs` draws a bg track + a fill rect `fraction()×width` via `DrawRect` [corner_radius/border from the UI SDF pipeline], one Z sub-layer above the track; reflect+clone+serde+editor-add registered like the other widgets; example `ui_progress`), **Tooltip** (hover popup on any `UiNode` widget: `{text, delay_secs, fade_secs, font_size, text/bg/border colors, corner_radius, border, padding, offset [from cursor], size_override}` + private hover timer; `system/tooltip_pass.rs` [runs LAST so tooltip text draws over other widget text] accumulates hover — cursor in the node rect AND not covered by a pointer-opaque surface drawn above it (`PointerCapture::occludes`; the host widget itself need not be opaque, so Label/ProgressBar tooltips work) — then after `delay_secs` fades a box in near the cursor at `TOOLTIP_Z`=100, viewport-clamped [right overflow slides left, bottom overflow flips above the cursor]; box auto-sizes via a shaped-width ESTIMATE [0.5 em/ASCII, 1 em/CJK char, 1.2×line height, `\n` breaks; exact shaping is render-time] — `with_size` pins it exactly; empty `text` disables; **`InputState::set_cursor` is now `pub`** [virtual/gamepad cursor + synthetic hover in headless]; reflect+clone+serde+editor-add registered; example `ui_tooltip`), **Dropdown** (combobox: `{items, selected, open [transient, not saved], bg/hover/text colors, font_size, corner_radius, item_height [0 = node height]}`; PRESS opens the list below the box [press-not-click -> native one-gesture press-drag-release select via a transient `press_opened` flag; at most ONE list open — Enter-opening closes the others like a pointer press-away; flips ABOVE at the viewport bottom — `flips_up`/`list_pos`/`expanded_rect` are the single geometry source shared by render + click rows + capture], click a row selects + closes + emits `UiEvent::DropdownChanged(entity, index)` [only on actual change], press-away closes; while open the whole box+list registers in `PointerCapture` at `DROPDOWN_LIST_Z`=90 [< TOOLTIP_Z] so covered widgets don't fire — and (since text z-ordering) their labels are hidden too; focusable — Enter/Space/A toggles open, ←/→ steps selection directly [clamped, Slider-like] so a settings row works pad-only; empty `items` never opens; `system/dropdown_pass.rs` runs after checkbox, before tooltip; reflect+clone+serde+editor-add registered; example `ui_dropdown`), **RadioGroup** (radio buttons, ONE entity = the whole group [options can never disagree]: `{items, selected, circle/fill/hover/text colors, font_size, circle_size, item_height [0 = node height / n]}`; click a row selects + emits `UiEvent::RadioChanged(entity, index)` [only on actual change; drag-off cancels — CheckBox-style press+release ownership]; rows render ring [SDF rounded rect at full radius = circle] + selected dot + label + hover tint, `row_at` = single geometry source [rows clickable only inside the node rect = the capture surface]; focusable as ONE stop — ←/→ steps selection [clamped, Slider-like, emits `RadioChanged`]; reflect+clone+serde+editor-add registered; example `ui_radio`), **TabBar** (equal-width tab headers, ONE active: `{tabs, selected, bg/active/hover/text/active_text colors, font_size, corner_radius, gap}`; click a header selects + emits `UiEvent::TabChanged(entity, index)` [only on actual change; gaps select nothing; drag-off cancels]; headers = active/hover/inactive bg + centered title, `tab_width`/`tab_rect`/`tab_at` = single geometry source; bar renders HEADERS ONLY — the game toggles per-tab widgets' `UiNode.visible` [hidden widgets already skipped by Tab-focus, so nav never lands on inactive-tab content]; focusable as ONE stop — ←/→ steps the active tab; reflect+clone+serde+editor-add registered; example `ui_tabs`), **ListBox** (scrollable selectable single-column list — inventory / level-select / file lists: `{items, selected, scroll_offset [transient, not saved], row_height, font_size, bg/hover/selected/text/border colors, corner_radius, border}`; the wheel scrolls the list under the pointer, clicking a *visible* row selects it + emits `UiEvent::ListBoxChanged(entity, index)` [only on actual change; drag-off cancels — CheckBox-style press+release ownership]; scrolls when items overflow the node; `row_at` [scroll-aware] = single geometry source, highlight rects clamped + labels bottom-clipped to the node rect so a partly-scrolled row never spills [the text scissor top = text top, so the top partial row's label is dropped, its highlight kept]; focusable as ONE stop — **↑/↓** (keyboard only, natural for a vertical list) or **←/→** (keyboard/D-pad/stick) step selection [clamped, Slider-like] + auto-scroll it into view via `scroll_to_selected`; `system/list_box_pass.rs` runs after TabBar, before Dropdown; new `InputSnapshot.nav_up`/`nav_down` (keyboard arrows) consumed only here; reflect+clone+serde+editor-add registered; example `ui_list_box` [12-item inventory scrolls + custom-styled level select]), **Stepper** (numeric +/- spinner — quantity / settings values: `{value, min, max, step, decimals, font_size, bg/button/button_hover/text/border colors, corner_radius, border}`, value-based like `Slider` [a bounded `f32`] but stepped DISCRETELY by `step` rather than dragged, and persists `value` directly [no `initial_value` split]; the node rect splits into a `-` button / centered value label / `+` button [`zone_at`/`button_width` = single geometry source; button width = node height capped at ⅓ node width], click `-`/`+` steps `value` by `step` clamped to `min..=max` + emits NEW `UiEvent::StepperChanged(entity, f32)` [f32 like `SliderChanged`, NOT an index; only on actual change — a click at a bound is silent; CheckBox-style press+release ownership, drag-off cancels]; `clamped_value` uses `max/min` not `f32::clamp` so a reflect-edited `min>max` can't panic; `stepper_pass.rs` runs after ListBox, before Dropdown; focusable as ONE stop — ←/→ step [clamped]; NO transient state [buttons recompute hover each frame — cleaner than Dropdown]; reflect+clone+serde+editor-add registered; example `ui_stepper` [volume 0..100/5 · difficulty 1..5 · custom-styled zoom 0.5..3 step .25 decimals 2]), **Switch** (styled boolean toggle — settings on/off, the switch-look alternative to `CheckBox`: `{on, label, track_width/track_height, on/off/knob/text colors, font_size}`; a sliding pill track + round knob [left=off / right=on], track colored by state, optional label to the right; clicking ANYWHERE on the node flips `on` + emits NEW `UiEvent::SwitchToggled(entity, bool)` [CheckBox-style press+release ownership, drag-off cancels; a click always flips so every click emits], `track_rect`/`knob_rect` = single geometry source [pill radius = track_h/2, knob = a circle padded inside]; focusable as ONE stop — Enter/Space toggles [like CheckBox] AND ←/→ set off/on ABSOLUTELY [emit only on actual change — a 2-position-slider feel, more pad/keyboard-operable than CheckBox]; `switch_pass.rs` runs after Stepper, before Dropdown; NO transient state; reflect+clone+serde+editor-add registered; example `ui_switch` [Sound/Music/Fullscreen + custom-green larger Vsync + live readout]) | `src/ui/slider.rs`, `src/ui/checkbox.rs`, `src/ui/progress_bar.rs`, `src/ui/tooltip.rs`, `src/ui/dropdown.rs`, `src/ui/radio_group.rs`, `src/ui/tab_bar.rs`, `src/ui/list_box.rs`, `src/ui/stepper.rs`, `src/ui/switch.rs` |
| LocalizedText (key bound to a widget), LocalizationSystem (resolves `LocaleResource::t` into Label/Button/CheckBox each frame) | `src/ui/localized.rs` |
| Tag, EntityDef (+ `components` map), SceneDef, Prefab, spawn_entity_def, spawn_scene_def, **SerdeComponentRegistry** + `App::register_serde_component::<T>` (any serde component persists to scene RON; UI widgets + `AnimationStateMachine`/`Timeline`/`CameraTarget` auto-registered; `SerdeComponentRegistry` now lives in `src/serde_registry.rs`, re-exported from prefab) | `src/prefab.rs`, `src/serde_registry.rs` |
| Timer, **`Tween<T: Lerp = f32>`** (generic over the value type — `Tween<Vec2>`/`Tween<Color>`; default `f32` keeps old call sites + `TweenSequence` unchanged), TweenSequence (chained multi-segment `f32` tweens: per-segment easing + `looping`, carries leftover `dt` across segments; example `tween_sequence`), **Easing** (`#[non_exhaustive]`; Linear/EaseIn/EaseOut/EaseInOut/EaseInBack/EaseOutBack/EaseInBounce/EaseOutBounce/EaseInElastic/EaseOutElastic), Lerp (general interpolation trait); game-feel example `juice_demo` (hit-stop + shake + easing + fade) | `src/timer.rs`, `src/tween.rs` |
| **DialogueBox**, **DialogueChoice**, **DialogueSystem** (speaker + typewriter text box: per-line reveal at `chars_per_sec`, two-stage `advance()`, optional `portrait`, UTF-8-safe; **localization** — `localized(speaker_key, line_keys)` + `resolve(&LocaleResource)` fill `lines`/`speaker` from keys WITHOUT touching reveal, re-resolved each frame by `DialogueSystem` so live `set_locale` retranslates; **branching** — `choices: Vec<(line, Vec<DialogueChoice>)>`/`with_choices`/`pending_choices`/`choose(i)` jump to `goto` [OOB clamps to end], `advance()` no-ops while a decision pends); **data-driven (`tree.rs`)** — `DialogueTree` (ordered named nodes, line literal-or-`line_key`, `goto`-by-id choices) flattens to a `DialogueBox` (node order = line index); `DialogueRegistry` + `App::load_dialogue(name, path)` load+hot-reload like clip/particle sets; **conditional/effect (`vars.rs`)** — `DialogueVars` resource of `DialogueValue` (Bool/Int/Float/Str); `DialogueChoice.cond: DialogueCond` (Eq/Ne/Gt/Lt/Ge/Le) gates visibility, `.effect: DialogueEffect` (SetVar / EmitEvent→`Events<DialogueEvent>`); world-level `dialogue::advance`/`choose` honor conds + apply effects via `visible_choices`/`is_choosing`/`advance_with`/`choose_visible` (plain `advance`/`choose` stay for the no-vars case); RON authors cond/effect inline via IMPLICIT_SOME; builders `when`/`then`; **render** — `DialogueSystem` draws speaker+text via `TextQueue` AND, when a box sets `portrait` (`with_portrait`, `Handle<ImageAsset>`), draws it left via `UiImageQueue` + shifts the text right (no portrait = original left-margin layout, byte-identical); **styling** — `DialogueStyle` (opt-in resource: layout offsets/font sizes/colors for speaker/body/choices/hint/portrait + fallback viewport; vertical positions are offsets-up-from-bottom; absent = `default()` reproduces the original look exactly, so non-breaking) restyles `DialogueSystem` without forking; examples `dialogue_demo`/`dialogue_portrait` (per-speaker portraits)/`dialogue_branching` (live en↔ko)/`dialogue_quest` (RON tree + cond + event hooks)/`dialogue_style` (T toggles a custom `DialogueStyle`)) | `src/dialogue/` (`mod.rs` re-export coordinator + `model.rs` [`DialogueBox`/`DialogueChoice` + `advance`/`choose`], `style.rs` [`DialogueStyle`], `system.rs` [`DialogueSystem`], `tree.rs`, `vars.rs`) |
| Coroutine, CoroutineRunner, CoroutineSystem (imperative timed-action sequencer — `wait(secs)` / `run(\|&mut World\|)` / `run_for(dur, \|&mut World, t\|)` steps chained via a builder; `CoroutineSystem` removes the `CoroutineRunner` resource, ticks all coroutines passing `&mut World` to the closures, then reinserts — closures must not re-enter the runner; carries leftover `dt` across steps; distinct from `Timeline` (data keyframes) / `TweenSequence` (value interp); example `coroutine_demo`) | `src/coroutine.rs` |
| Timeline, Track, Keyframe, TimelineSystem (keyframe cutscenes → entity Transform/Sprite; **CameraTarget** marker + `zoom` track route a timeline into the **Camera** resource as a virtual rig; `Track::add`/`keyframes`/`len`/`remove`/`set_time`/`set_value`/`set_easing`/`clear` editor accessors+edit ops drive the docked **Timeline panel** (per-track keyframe list + add-keyframe + per-type value editing + playback controls); example: `timeline_cutscene`) | `src/timeline.rs` |
| History (generic snapshot undo/redo for grid puzzles, turn-based, editors) | `src/history.rs` |
| ParticleEmitter (+ **`gravity: Vec2`** per-particle accel + **`emit_shape: EmitShape`** Point/Circle/Ring/Box spawn scatter — `with_gravity`/`with_emit_shape`; additive, ZERO/Point = prior behavior; example `particles_showcase`) + **`max_per_frame: u32`** (per-frame spawn cap / runaway guard, `with_max_per_frame`, default `DEFAULT_MAX_PER_FRAME`=64 [re-exported]; raise for dense rain/snow whose `spawn_rate > cap*fps` — else it silently under-emits; also a RON config field + editor tuner row; example `particle_spawn_cap`), ParticleSystem, ParticleBurst (one-shot burst + `ParticleEmitter::burst()`); ParticleConfigSet/ParticleConfigRegistry/ClipSetError-style ParticleConfigError (data-driven emitter configs from RON, **incl. `gravity` + `emit_shape`** [`emit_shape: Circle(radius: 10.0)` etc.]; `App::load_particle_configs` = registry + DataTable-style hot-reload); **`GpuParticleEmitter` also mirrors `gravity` (per-particle, integrated in the compute shader) + `emit_shape` (CPU-sampled at emit)** — `src/gpu_particle.rs` + `src/renderer/gpu_particle.rs` (80-byte `GpuParticle`); **`ParticleConfigSet::gpu_emitter(name)`** = RON→`GpuParticleEmitter` builder (native-only; mirrors `emitter()`, square `size`=width `size.0`, texture/z ignored), example `gpu_particles` loads `gpu_particles.ron` via `load_particle_configs`+`gpu_emitter` | `src/particle/` (`mod.rs`, `config_set.rs`) |
| Tilemap (+ runtime `set_tile`/`get_tile`/`cell_at_world`/`cell_center_world`/`cell_z`/`cell_render_size`/`dims`; **`TilemapProjection` (`#[non_exhaustive]`) Orthographic[default]/Isometric/Hexagonal/HexagonalFlat** via `with_projection` — iso = 2:1 diamond [picking = inverse diamond + round, depth-sorted `z=row+col`]; hex = pointy-top odd-r offset [`cell_center_world` row pitch `ts·√3/2` + odd-row half-shift, picking = pixel→axial→cube-round, `z=-1` no overlap, taller sprite via `cell_render_size`]; **HexagonalFlat = flat-top odd-q offset** [90°-rotated mirror: col pitch `ts·√3/2` + odd-col half-shift-down, `ts`=flat-to-flat height, wider sprite `ts·2/√3 × ts`, flat-top pixel→axial picking]; all branch in `cell_center_world`/`cell_at_world`/`cell_z`/`cell_render_size`; **per-map render depth `z`/`with_z`** (default `-1.0`; ortho/hex share it so two tilemaps stack as background+foreground layers — `cell_z` returns `self.z`; iso ignores it, keeps per-cell `row+col`; example `tilemap_layers`); examples `iso_tilemap`/`hex_tilemap`/`hex_tilemap_flat`), TilemapAtlas, TilemapSystem (reactive — diffs cached grid, updates only changed cells; places tiles via `cell_center_world`+`cell_render_size`+`cell_z`), **TilemapAutotile** (neighbor-bitmask autotiling; ONE component, `mode: AutotileMode` = `Single{mask_to_tile}` [any-nonzero connects: `edge_16`/`blob_47`] or `Multi{rules: Vec<TerrainRule>}` [per-terrain same-value: `multi_edge_16`]; `Neighborhood::Edge4`/`Blob8` (square — also work on **iso**, same `tiles[][]` topology) + **`Hex6`** (pointy odd-r) / **`Hex6Flat`** (flat odd-q) hex neighborhoods (6 parity-aware nbrs, mask 0..64) + `hex_6`/`hex_6_flat` 64-tile constructors, `with_oob_filled`, `compute_tile_mask`/`compute_tile_mask_typed`; unified from the old separate `MultiTerrainAutotile`, ghost `ConnectRule` dropped — v0.30.0; iso+hex autotile + `hex_autotile`/`hex_autotile_flat` examples — v0.39.0; **64-tile hex blob atlas** v0.43.0 [`gen_hex_autotile_sheet` procedurally writes `examples/assets/hex_autotile{,_flat}.png` where tile index == the Hex6/Hex6Flat mask, the hex analogue of square `blob_47`; full-blob examples `hex_blob_autotile`/`hex_blob_autotile_flat`]); **animated tiles**: TileAnimation (frames + `frame_time`, `frame_at`) + TileAnimationSet (tilemap-entity component mapping tile value → animation; **`stagger`/`with_stagger`** = per-cell phase offset factor, default `DEFAULT_TILE_ANIM_STAGGER`=0.37 [was a hardcoded literal]: each cell starts at phase `(row+col)·frame_time·stagger`, `0.0` = all cells lockstep, higher = more rippling; example `tile_anim_stagger` synced-vs-staggered) + AnimatedTileCell (per-tile-entity tag w/ precomputed frame UVs) + AnimatedTileSystem (cycles tagged cells' UvRect each frame — render-only, does NOT bump generation, so the non-animated fast path is untouched; `TilemapSystem` tags animated cells at spawn via `stagger_phase`; example `animated_tiles`) | `src/tilemap/` (`mod.rs` data model, `autotile.rs`, `system.rs`, `animation.rs`) |
| AudioManager (playback, positional audio, bus mixer (`assign_bus`/`set_bus_volume`/`bus_volume`/`bus_names` — `bus_names` lists all buses for the editor mixer panel), fades + **track-to-track crossfade** (`crossfade` — fades the old track out + the new track in via an internal temp channel, reusing the `Fade`/`update` infra; example `music_crossfade`); **ducking/sidechain** via `duck_bus`/`release_bus`/`bus_duck`/`set_sidechain`/`clear_sidechain` — BusDuck/Sidechain), AudioSystem (built-in system that ticks `update(dt)` so fades + ducks progress; SFX file-bytes cache); **AudioManager is native-only** (`cfg(not(wasm32))`, rodio) — wasm has **`WebAudio`** (`src/audio_wasm.rs`, wasm-only: Web Audio `AudioContext` — `new`/`play(bytes)` fire-and-forget SFX + **synthesized tone** `play_tone(freq,dur,vol)`/`play_tone_on_bus` (`OscillatorNode`→gain attack/release envelope, no clip bytes) + **named tone channels** `play_tone_on_channel(ch,freq,dur,vol,bus)`/`is_channel_playing(ch)`/`set_low_pass(ch,hz)`/`clear_low_pass(ch)` (tracked per-channel `OscillatorNode` + optional `BiquadFilterNode` low-pass applied on next play — replay cuts the prior tone) + **controllable per-source SFX** `play_sfx(bytes) -> Sfx` (source→`StereoPannerNode`→per-source `GainNode`→master; `Sfx::set_pan`/`set_volume`/`is_playing`/`stop`, panner+gain created sync so set_pan/volume work pre-decode) + **master GainNode** `set_volume`/`volume` (all playback routes through it) + **looping music channel** `play_music`/`stop_music` (single channel) + **track-to-track crossfade** `crossfade_music` (audio-clock `linear_ramp_to_value_at_time` on per-music gain nodes — no per-frame `update()`; no-current-track = fade-in) + `suspend`/`resume` pause-all + **state accessors** `is_running` / `is_music_playing` + **named mixer buses** `set_bus_volume`/`bus_volume`/`bus_names` + `play_on_bus`/`play_sfx_on_bus` + **manual ducking** `duck_bus`/`release_bus`/`bus_duck` (a bus = a `duck → volume → master` 2-gain chain; sounds connect to `duck`, ducking ramps `duck` independent of `volume`; lazy-created, volume-only buses persist; audio-clock ramps, no per-frame `update()`, `dur<=0`=instant `set_value`) + **2D positional** `play_at(bytes, source, listener, max_dist) -> Sfx` (+ `play_at_on_bus(…, bus)` = positional routed through a named bus) + `Sfx::update_position`/`volume`/`pan` (vol = 1−clamp(dist/max), pan = clamp(dx/max), native parity; `play_at` routes to master) + **named positional channels** `play_at_on_channel`/`update_position`/`stop_channel` (a looping `Sfx`+panner tracked by name in `spatial_channels`; backs the facade's tracked positional); **automatic sidechain stays native-only** (needs continuous trigger-activity eval — drive ducking manually instead); example `web_audio` (wasm-only — drives the whole surface incl. `play_sfx` + buses + crossfade + ducking + positional + self-checks the lifecycle, see `scripts/wasm_audio_smoke.sh`)); **cross-platform `Audio` facade** (`src/audio_facade.rs`, un-gated) = ONE API over native `AudioManager` / wasm `WebAudio` so a dual-target game writes audio with **ZERO `cfg` guards** — intersection API (`play_sfx`/`play_sfx_on_bus`, **synthesized** `play_tone`/`play_tone_on_bus` [sine, no clip bytes — native rodio synth / wasm `OscillatorNode`], **named tone channels** `play_tone_on_channel`/`is_channel_playing`/`set_low_pass`/`clear_low_pass` [sustained/trackable tone + low-pass filter — native `AudioEffect.low_pass_hz` / wasm `BiquadFilterNode`, applied on next play], **tracked 2D positional** `play_at_on_channel`/`update_position`/`stop_channel` [a LOOPING positional sound on a named channel whose dist-volume + stereo pan track a moving source each frame — native `AudioManager::play_bytes_at` + positional channel / wasm looping `Sfx`+`StereoPannerNode` kept in a `spatial_channels` map], `play_music`/`crossfade_music`/`stop_music`, `set_master_volume`, `set_bus_volume`/`bus_volume`, `duck_bus`/`release_bus`/`bus_duck`, `resume` [web `AudioContext` unlock after a gesture; native no-op], `update` [native fades/ducks tick; web no-op]) + `AudioFacadeSystem`; native routes unrouted SFX/music/tones through a conventional `"master"` bus (round-robin 16-voice SFX channels, shared by `play_sfx`+`play_tone`) so a **named-bus sound bypasses `set_master_volume` on native** (native buses don't nest — documented); backed by additive `AudioManager::play_bytes`/`crossfade_bytes`; **excludes** UNTRACKED positional one-shots (`play_at`) + per-channel effects beyond low-pass (pitch/attack/release) + automatic sidechain (native-only, use the backend directly); examples `audio_facade` (native + web, same code — incl. named-channel BGM + low-pass via `G`/`L`) + `positional_audio` (orbiting source + arrow-key listener move, native+web) + games `survivor`/`shooter`/`settings_menu` adopt the facade (cfg-guard-free audio — sfx now play on web too); **codec decode coverage** (`codec_decode_tests` in `src/audio/playback.rs`) decodes committed synthesized CC0 fixtures `src/audio/fixtures/{tone.wav,tone.ogg,tone.mp3}` (a 440 Hz sine, regen recipe in `fixtures/README.md`) through `rodio::Decoder` to pin the enabled `wav`/`vorbis`/`mp3` features — device-free so it runs on CI; a dropped feature reds the matching test (added because the rodio 0.22 symphonia swap left `.ogg`/vorbis exercised by nothing) | `src/audio.rs`, `src/audio/`, `src/audio_wasm.rs`, `src/audio_facade.rs` |
| save / load (AEAD, player saves) / write_ron / read_ron (plaintext, design-time assets) / load_or_default / exists / delete / save_path / SaveError; **save_versioned + load_migrated + SaveMigrator** (versioned-schema save migration; **envelope keeps the payload as RON text since 0.116 [EW-006]** so data-carrying enum variants round-trip when no migration runs — migration steps still operate on a `ron::Value`, which can't represent enums, so keep enum fields stable across schema versions; pre-0.116 tree envelopes load via a legacy path; example `save_migration` [Space re-save carries `GameMode::Custom{multiplier}`]); **wasm parity: ALL of `save`/`load`/`save_versioned`/`load_migrated` + `write_ron`/`read_ron`/`exists`/`delete` work on wasm** via `localStorage` (internal `wasm_storage`, web-sys `Storage`) — the AEAD core (ChaCha20-Poly1305) is cross-platform; on wasm the encrypted blob is **hex-encoded** into localStorage, nonce RNG is `OsRng` (getrandom js); localStorage is inspectable so the embedded key = obfuscation+tamper-detection, not secrecy (same as the native file); the wasm AEAD localStorage round-trip is **browser-verified** by example `wasm_save` + `scripts/wasm_save_smoke.sh` (headless 7/7); examples `save_counter` (plaintext), `save_encrypted` (AEAD, native), `wasm_save` (wasm AEAD localStorage self-check) | `src/save.rs` |
| NetworkClient (native=tungstenite thread, wasm=web-sys), NetworkSystem (polls → `Events<NetworkEvent>`), NetworkEvent, NetworkConfig (queue/size caps + **`read_timeout`** = native client-thread socket poll interval / outbound-flush latency, default `DEFAULT_READ_TIMEOUT`=5 ms [was a hardcoded literal], 1 ms floor, no-op on wasm; via `connect_with_config`; demoed in `mp_client`), RemoteEntities (`id→Entity` lifecycle), SnapshotBuffer<T: Lerp> (generic per-entity interpolation buffer); relay demo `mp_server`/`mp_client`, authoritative game `examples/games/coin_race`, client-prediction `examples/games/predict_shooter`, interpolation-only `examples/games/orbital_dodger`, AOI-streaming/interest-managed `examples/games/salvage_run` | `src/network.rs` |
| PostProcessConfig, PostProcessRenderer, **Tonemap** (HDR tone-mapping: `PostProcessConfig.hdr` renders the scene into an `Rgba16Float` intermediate so `>1.0` colours survive, then the post pass applies `tonemap` [None/Reinhard/AcesFilmic] + `exposure`; default off = byte-identical. HDR feeds the sprite + UI-primitive + shader-material + render-plugin **+ GPU-particle** passes (all format-matched — material/UI v0.59.0, GPU particles v0.62.2: `GpuParticleRenderer` lazily builds + caches a render pipeline per non-surface target format, mirroring the sprite cache). Examples `tonemap`, `gpu_particles` (H toggles HDR post)); **real multi-pass bloom** (`PostProcessConfig.bloom` opt-in [+ `bloom_iterations` 0..=8 = **mip-pyramid depth** since v0.67.0], requires `enabled`; **mip-chain "dual filter"** [Jimenez/CoD]: bright-pass → 13-tap downsample chain (mip pyramid) → 3×3-tent additive upsample chain → additive composite onto the scene intermediate *before* post, which then skips its inline 4-tap via the `bloom_enabled` uniform flag; wider/smoother/energy-preserving glow vs the old fixed-half-res separable Gaussian — same `pub(crate)` API, so frame.rs/post_lighting.rs unchanged; `BloomRenderer` in `src/renderer/bloom.rs`, **single-target reconfigure** like post/lighting [NOT the per-format pipeline-cache], format-matched to the scene intermediate so it works under HDR; `false` default = byte-identical inline bloom; example `bloom`) | `src/renderer/post_process.rs`, `src/renderer/bloom.rs` |
| PointLight, AmbientLight, LightingRenderer (2D point-light pass, nearest-to-camera cull; native-only); **LightingConfig** (opt-in `max_lights` cap, default `DEFAULT_MAX_LIGHTS`=16 — was a hardcoded hard cap; raises the GPU `array<GpuLight,N>` length [baked at shader build] + uniform size [`32 + N*32` B, runtime-sized via a 32-B `LightingHeader` + dynamic lights region]; a runtime cap change rebuilds the renderer; example `lighting_cap` (40-light grid, SPACE toggles 16↔40, `LIGHTING_CAP` env + `scripts/lighting_cap_smoke.sh` headless A/B)) | `src/renderer/lighting.rs`, `src/resources/render.rs` |
| RenderTarget, OffscreenCamera, create_render_target (render-to-texture; each offscreen target submits its **own** command buffer so it uses its own camera — exclude RT-display sprites via `layer_mask`; example: `security_camera`); **`App::create_render_target_with_format(name,w,h,format)`** = caller-chosen pixel format (e.g. `wgpu::TextureFormat::Rgba16Float` HDR, or linear `Rgba8Unorm`); **`App::create_render_target_with_filter(name,w,h,filter)`** (+ `RenderTarget::new_with_filter`, `filter()`) = caller-chosen display sampler `FilterMode` (default `Nearest`; `Linear` smooths a scaled/blurred RT — the RT's `sampler`/`bind_group` is what a display sprite samples; example `render_target_filter`); `RenderTarget` stores its `format()`; the offscreen pass threads the RT's format into a **format-matched sprite pipeline** — `SpriteRenderer` lazily builds + caches an extra sprite pipeline per non-surface target format (`extra_sprite_pipelines`, keyed by format; the surface-format fast path is untouched, no per-frame cost), so an `OffscreenCamera` renders correctly into an HDR/linear RT (no wgpu format-mismatch). Tone-mapping an HDR RT for display is the game's job (the example uses an exposure-multiply on the display sprite's colour — sprite `Color` is f32, un-clamped, so values >1.0 reach the shader). **Material + UI pipelines are also format-matched** (v0.59.0; `MaterialRenderer.custom_pipelines` keyed by `(hash, format)`, `extra_ui_pipelines` per format) — a `ShaderMaterial` in an offscreen non-surface RT renders (example `offscreen_material`); UI primitives render through the HDR post intermediate. Examples `hdr_render_target` (over-bright scene → `Rgba16Float` vs default 8-bit RT side by side; lower exposure → HDR keeps core-vs-mid distinct, 8-bit collapses them). **Renderability query + fallback** (v0.65.0): **`RenderCapabilities`** resource (re-exported, inserted at GPU init) — `supports_render_target(format)` / `surface_format()` let a system/`on_enter` ask whether a format (e.g. `Rgba16Float`) is a usable color render target *before* requesting it (float RTs need `EXT_color_buffer_float` on WebGL2); `create_render_target_with_format` also **auto-falls-back** to the surface format + warns when a requested format isn't renderable (`GpuContext::resolve_render_target_format`, backed by the now-retained `adapter`); example `render_format_query` | `src/renderer/render_target.rs`, `src/renderer/context.rs`, `src/renderer/sprite.rs`, `src/app/render/offscreen.rs`, `src/app/assets.rs`, `src/components.rs` |
| **RenderPlugin** trait + `App::add_render_plugin` (fork-friendly custom render-pass hook — `record(ctx: &mut FrameContext, world, viewport)` runs per-frame after the sprite/UI/particle passes, before post/lighting; `FrameContext.format` lets a plugin build its own pipeline; additive — no-op when none registered; native+wasm; example `render_plugin`) | `src/renderer/render_plugin.rs`, `src/app/render/frame.rs` (dispatch) |
| **ShaderMaterial** (per-entity custom fragment shader component — attach to an entity to replace the built-in sprite frag shader; bindings `@group(1)` texture/sampler + `@group(2)` `params: vec4<f32>`; renderer compiles + caches one pipeline per source hash via `MaterialRenderer`; example `shader_material`) | `src/material.rs`, `src/renderer/sprite/material.rs` |
| **NineSlice** (9-patch scalable sprite component — `border: [f32;4]` world-px + `uv_border: [f32;4]` UV-fraction, both `[left,right,top,bottom]`; the sprite pass emits 9 sub-quads (corners fixed-size, edges/center stretched) instead of 1 when a `NineSlice` is present, additive — ordinary sprites unchanged; rotates rigidly; not for `AtlasSprite`/`ShaderMaterial`; example `nine_slice`) | `src/nine_slice.rs`, `src/renderer/sprite.rs` (9-quad branch) |
| DrawText, TextQueue, TextAlign (Left/Center/Right/**End**/**Auto** — Auto right-aligns RTL automatically), TextAnchor (screen-space text, top-left origin; `DrawText::centered` anchors at text center; place at a world position via `Camera::world_to_screen`); **bidi/RTL shaping is built in** (`Shaping::Advanced`); `ExtraFonts` resource (multi-script font fallback alongside `FontData`); **text z-ordering** — `DrawText.z: Option<f32>` + `with_z`: `None` [default] = the historical final on-top pass [right for HUD; unaffected by post]; `Some(z)` composites the text AMONG the UI rects/images at that z [frame.rs partitions the queue, uploads the sorted primitives ONCE (`prepare_ui_primitives`) and alternates instance-range passes with pooled glyphon batches (`render_ui_primitive_range`/`TextRenderer::render_batch`, `interleave_runs` in `text/layering.rs`; per-format atlas pools = the format-matched-pipeline pattern; `end_frame` trims/resets once per frame); tie = text over the surface; layered text renders pre-post so HDR grades it WITH its widget; the widget passes set label z automatically → an open dropdown list/tooltip/panel now hides covered labels (the old text-after-rects bleed is FIXED); render test `layered_text_is_covered_by_higher_z_rect`]; example `text_layers` (overlay card hides a lower caption, Space raises/lowers); examples `rtl_text` (Hebrew + multi-font), `centered_text` (`DrawText::centered` off-center-x visual check — guide lines through each `position.x`, EW-001 demo; **also ships to the web** — `examples/centered_text/web/` + headless render smoke `scripts/centered_text_smoke.sh`) | `src/renderer/text.rs` |
| wgpu render pipeline (rarely edited directly) | `src/renderer/` |

---

## Core patterns & task recipes

Detailed in **`docs/PATTERNS.md`**:

- **Architecture patterns** — ECS query API (`query2`/`query_opt2`), borrow-checker
  workaround (collect entities then `get_mut`), render-layer separation
  (`AnimationSystem` → `UvRect` → renderer), **render-target-format-aware pipeline cache**
  (a new render pass keys its pipeline by *target* format, not `gpu.config.format`, so it
  survives an offscreen/HDR RT — else it silently vanishes; sprite/UI/material/GPU-particle),
  UI system order (`LayoutSystem` before `UiSystem`), animation state-machine order
  (`StateMachineSystem` after `AnimationSystem`), `PhysicsWorld` encapsulation accessors.
- **Task recipes** — adding a component / system / resource / event, and scene transitions.

---

## Agent working notes

Context-management heuristics (when to split work into a subagent by task type),
efficient-exploration order, and subagent-prompt principles → **`docs/AGENT_NOTES.md`**.

---

## Documentation rules

- **Language**: Write doc prose in **English** to minimize token cost (≈ ⅓ the tokens of
  Korean). Code, file paths, identifier tables, and API names stay as written.
- **Exceptions kept in Korean**: the beginner glossary (`docs/ENGINE_TERMS_FOR_BEGINNERS.md`)
  and personal/gitignored one-off prompt or plan docs.
- New `docs/HANDOFF.md` entries are written in English.
- **Length**: keep `CLAUDE.md` / `AGENTS.md` ≤200 lines. Prefer concision, but **do not
  drop needed content to hit the limit** — when trimming would risk losing information,
  move the detail into a new `docs/*.md` (e.g. `docs/PATTERNS.md`, `docs/SUBSYSTEM.md`)
  and leave a one-line reference here.

---

## Document map

| Document | Purpose |
|------|------|
| `CLAUDE.md` (this file) | Agent quick reference — module map, task checklists |
| `docs/PATTERNS.md` | Core architecture patterns + task recipes (extracted from this file) |
| `docs/AGENT_NOTES.md` | Agent working heuristics — context-management split, exploration order, subagent-prompt principles (extracted from this file) |
| `docs/MACOS_FFI.md` | How to add an objc2 Apple-framework binding (version-pin, discover the API from registry source, feature flags) — e.g. the macOS GameController gamepad backend |
| `docs/RENDER_TESTING.md` | CI-verifying the GPU render path — `tests/render.rs` + the lavapipe `render` job |
| `REFERENCE.html` | Full public API + code examples (detailed) |
| `docs/HANDOFF.md` | Per-phase dev history, background on architecture decisions |
