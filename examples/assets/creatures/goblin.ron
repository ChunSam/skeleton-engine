// A custom game-config type loaded via the public RonRegistry<T> + RonLoadable.
// Edit a value and press R in the running example to hot-reload it from disk.
(
    name: "Goblin",
    hp: 30,
    speed: 120.0,
    r: 0.45,
    g: 0.82,
    b: 0.35,
)
